# StylingComparison

Projeto React Native criado para comparar estilos usando **StyleSheet** vs **Styled Components**.

---

## 📁 Estrutura do projeto

- `App.js` — implementação com **StyleSheet** (ou Styled Components, se alterado)  
- `package.json` — dependências do projeto  
- `node_modules/` — ignorada pelo `.gitignore`  
- `.expo/` — cache do Expo  
- `assets/` — imagens e recursos do projeto

---

## 🚀 Como executar

1. Clona o repositório:
```bash
git clone <URL_DO_REPOSITORIO>
cd StylingComparison
```

2. Instala as dependências:
```bash
npm install
```

3. Inicia o projeto com Expo:
```bash
npx expo start

Dica: se houver problemas de dependências, usa:
yarn add styled-components
```

⚡ Funcionalidades

Tela com Container, Título e Botão

Duas implementações possíveis:

StyleSheet (padrão React Native)

Styled Components (mais moderno e legível)
